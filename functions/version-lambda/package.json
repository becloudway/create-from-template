{
  "name": "version-lambda",
  "version": "0.0.1",
  "main": "src/index",
  "files": [
    "src/"
  ],
  "scripts": {
    "test": "jest --config ../../jest.config.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "tsc": "tsc -p tsconfig.json",
    "build": "package-single-lambda",
    "clean": "rm -rf ./.turbo && rm -rf tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@cloudway-template/config": "workspace:^",
    "@cloudway-template/scripts": "workspace:^",
    "eslint": "^8.23.1",
    "jest": "^29.0.3",
    "typescript": "4.8.3"
  }
}
